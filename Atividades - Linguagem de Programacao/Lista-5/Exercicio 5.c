#include <stdio.h>

int main(void) {
  unsigned int m, M;
  printf("Qual o numero do mes?\n");
  scanf("%d", &m);
  (m==1) ? (printf("O primeiro mes e Janeiro\n")) : (M);
  (m==2) ? (printf("O segundo mes e Fevereiro\n")) : (M);
  (m==3) ? (printf("O terceiro mes e Marco\n")) : (M);
  (m==4) ? (printf("O quarto mes e Abril\n")) : (M);
  (m==5) ? (printf("O quinto mes e Maio\n")) : (M);
  (m==6) ? (printf("O sexto mes e Junho\n")) : (M);
  (m==7) ? (printf("O setimo mes e Julho\n")) : (M);
  (m==8) ? (printf("O oitavoo mes e Agosto\n")) : (M);
  (m==9) ? (printf("O nono mes e Setembro\n")) : (M);
  (m==10) ? (printf("O decimo mes e Outubro\n")) : (M);
  (m==11) ? (printf("O decimo primeiro mes e Novembro\n")) : (M);
  (m==12) ? (printf("O decimo segundo mes e Dezembro\n")) : (M);
  return 0;
}
//Meses